package scalarnode

import (
	"gopkg.in/yaml.v3"
)

// Вот эту функцию надо написать
func ResultScalarNode(node []*yaml.Node, tmp []*yaml.Node, arch string) ([]*yaml.Node, error) {
	return node, nil
}
