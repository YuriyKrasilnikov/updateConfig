package mappingnode

import (
	"gopkg.in/yaml.v3"
)

// Вот эту функцию надо написать
func ResultMappingNode(node []*yaml.Node, tmp []*yaml.Node, arch string) ([]*yaml.Node, error) {
	return node, nil
}
